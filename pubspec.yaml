name: aerofreez_monitor
description: Monitor Flutter de Aero-Freez para Android
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  url_launcher: ^6.3.1
  google_fonts: ^6.3.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true